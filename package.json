{
  "name": "cofacts-ai-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test --passWithNoTests",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "@tanstack/eslint-config": "^0.3.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.16.0",
    "prettier": "^3.5.3",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
